spring:
  datasource:
    url: jdbc:mysql://localhost:8080/kfc
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
server:
  error:
    include-message: always
    include-binding-errors: always